---
phase: 04-twitch-chat-visual-customization-image-header-et-color-picker-pour-l-overlay-chat
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/TwitchVisual/TwitchVisualConfigTests.cs
autonomous: true
requirements:
  - TWITCH-V-01
  - TWITCH-V-02

must_haves:
  truths:
    - "TwitchVisualConfigTests.cs existe avec des tests RED qui compilent mais échouent"
    - "Les tests couvrent le round-trip JSON des 4 nouveaux champs TwitchSettings"
    - "Les tests vérifient que l'absence des champs dans un vieux JSON donne les valeurs par défaut"
  artifacts:
    - path: "LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/TwitchVisual/TwitchVisualConfigTests.cs"
      provides: "RED stubs TDD — contrat avant implémentation"
      exports: ["TwitchVisualConfigTests"]
  key_links:
    - from: "TwitchVisualConfigTests.cs"
      to: "LMUOverlay.Models.TwitchSettings"
      via: "accès direct aux propriétés HeaderImagePath, ShowHeader, BackgroundColor, AccentColor"
      pattern: "TwitchSettings.*HeaderImagePath"
---

<objective>
Créer les RED test stubs pour la Phase 4 avant toute implémentation.

Purpose: Le contrat TDD — les tests doivent être RED (compilables mais échouants) avant que le Plan 04-02 ne les rende GREEN. Miroir exact du pattern WebBrowserTests.cs de la Phase 3.
Output: LMUOverlay.Tests/TwitchVisual/TwitchVisualConfigTests.cs avec 2 tests RED couvrant TWITCH-V-01 et TWITCH-V-02.
</objective>

<execution_context>
@C:/Users/Romain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Romain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-twitch-chat-visual-customization-image-header-et-color-picker-pour-l-overlay-chat/04-CONTEXT.md
@.planning/phases/04-twitch-chat-visual-customization-image-header-et-color-picker-pour-l-overlay-chat/04-RESEARCH.md
@.planning/phases/04-twitch-chat-visual-customization-image-header-et-color-picker-pour-l-overlay-chat/04-VALIDATION.md

<interfaces>
<!-- Fichier test de référence à imiter exactement : WebBrowserTests.cs -->
<!-- LMUOverlay.Tests/WebBrowser/WebBrowserTests.cs -->
```csharp
using Xunit;
using LMUOverlay.Models;
using Newtonsoft.Json;

namespace LMUOverlay.Tests.WebBrowser
{
    [Trait("Category", "WebBrowser")]
    public class AppConfigWebBrowserTests
    {
        [Fact]
        public void AppConfig_WebBrowserProperty_SurvivesRoundTrip()
        {
            var config = new AppConfig();
            string json = JsonConvert.SerializeObject(config);
            var deserialized = JsonConvert.DeserializeObject<AppConfig>(json);
            Assert.NotNull(deserialized!.WebBrowser);
        }
    }
}
```

<!-- TwitchSettings actuel dans OverlayConfig.cs (lignes 71-75) -->
```csharp
public class TwitchSettings
{
    public string Channel     { get; set; } = "";
    public int    MaxMessages { get; set; } = 20;
    // MANQUANT — Phase 4 ajoutera : HeaderImagePath, ShowHeader, BackgroundColor, AccentColor
}
```

<!-- AppConfig dans OverlayConfig.cs (ligne 41) -->
```csharp
public AppConfig
{
    public TwitchSettings Twitch { get; set; } = new();
}
```
</interfaces>
</context>

<feature>
  <name>TwitchVisualConfigTests — RED stubs (Wave 0 TDD)</name>
  <files>LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/TwitchVisual/TwitchVisualConfigTests.cs</files>
  <behavior>
    Test 1 (TWITCH-V-01): TwitchSettings avec les 4 nouveaux champs survit au round-trip JSON
    - Créer AppConfig, assigner HeaderImagePath="C:\\test.png", ShowHeader=false, BackgroundColor="#1A1A2E", AccentColor="#9146FF"
    - Sérialiser en JSON, désérialiser, vérifier que les 4 valeurs sont préservées
    - DOIT ÉCHOUER en Wave 0 (les champs n'existent pas encore sur TwitchSettings)

    Test 2 (TWITCH-V-02): Un vieux JSON sans les nouveaux champs désérialise vers les valeurs par défaut C#
    - Désérialiser "{\"Twitch\":{\"Channel\":\"test\",\"MaxMessages\":20}}" (JSON sans les nouveaux champs)
    - Vérifier : HeaderImagePath == "", ShowHeader == true, BackgroundColor == "", AccentColor == ""
    - DOIT ÉCHOUER en Wave 0 (les champs n'existent pas encore sur TwitchSettings)
  </behavior>
  <implementation>
    RED phase only — NE PAS toucher TwitchSettings ni OverlayConfig.cs.
    Créer uniquement le fichier test dans un sous-dossier TwitchVisual/ (miroir du pattern WebBrowser/).
    Namespace: LMUOverlay.Tests.TwitchVisual
    Trait Category: "TwitchVisual"
    Usings: Xunit, LMUOverlay.Models, Newtonsoft.Json
    
    Cycle RED:
    1. Écrire TwitchVisualConfigTests.cs
    2. Vérifier que les tests compilent (les propriétés n'existent pas encore → erreur de compilation ou assertion fail)
    3. Si erreur de compilation : commenter les assertions problématiques avec "// RED: propriété absente" et laisser une Assert.True(false) explicite
    4. Commit: "test(04-01): add RED stubs TwitchVisualConfigTests"
  </implementation>
</feature>

<verification>
<automated>dotnet test LMUOverlay\LMUOverlay\LMUOverlay\LMUOverlay.Tests\ --filter "Category=TwitchVisual" --no-build</automated>
Les tests doivent ÉCHOUER (RED). Si le build échoue à cause de propriétés manquantes, remplacer les accès par Assert.True(false, "RED: TwitchSettings.HeaderImagePath missing") jusqu'à ce que le build passe mais les tests soient rouges.
</verification>

<success_criteria>
- TwitchVisual/TwitchVisualConfigTests.cs existe dans LMUOverlay.Tests
- Le projet test compile sans erreur
- Les 2 tests sont RED (échouent) avec un message clair indiquant ce qui manque
- Commit: "test(04-01): add RED stubs TwitchVisualConfigTests"
</success_criteria>

<output>
After completion, create `.planning/phases/04-twitch-chat-visual-customization-image-header-et-color-picker-pour-l-overlay-chat/04-01-SUMMARY.md`
</output>
