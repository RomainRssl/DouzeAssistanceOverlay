---
phase: 04-twitch-chat-visual-customization-image-header-et-color-picker-pour-l-overlay-chat
plan: 03
type: execute
wave: 2
depends_on:
  - "04-02"
files_modified:
  - LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay/Views/MainWindow.xaml.cs
autonomous: false
requirements:
  - TWITCH-V-03
  - TWITCH-V-04
  - TWITCH-V-05
  - TWITCH-V-06
  - TWITCH-V-07

must_haves:
  truths:
    - "Un bouton PARCOURIR apparaît dans la section TwitchChat de MainWindow"
    - "Cliquer PARCOURIR ouvre OpenFileDialog filtré PNG/JPG/BMP"
    - "Sélectionner une image met à jour l'overlay live sans redémarrage"
    - "Un toggle Masquer le bandeau cache header + séparateur immédiatement"
    - "Deux color pickers (Fond + Accent) avec swatches Twitch apparaissent"
    - "Un bouton Reset par couleur restaure la valeur sentinel vide"
    - "Tous les changements sont sauvegardés dans config.json immédiatement"
  artifacts:
    - path: "LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay/Views/MainWindow.xaml.cs"
      provides: "Section TwitchChat enrichie : image picker + toggle + 2 color pickers + 2 reset"
      contains: "PARCOURIR"
  key_links:
    - from: "MainWindow TwitchChat section"
      to: "TwitchChatOverlay.ApplyVisualSettings()"
      via: "_overlayManager.GetOverlay<TwitchChatOverlay>(\"TwitchChat\")?.ApplyVisualSettings()"
      pattern: "GetOverlay.*TwitchChat.*ApplyVisualSettings"
    - from: "Bouton PARCOURIR"
      to: "_config.Twitch.HeaderImagePath"
      via: "OpenFileDialog + _configService.Save(_config)"
      pattern: "OpenFileDialog.*HeaderImagePath"
    - from: "Toggle Masquer le bandeau"
      to: "_config.Twitch.ShowHeader"
      via: "AddToggle callback + _configService.Save + ApplyVisualSettings()"
      pattern: "ShowHeader.*Save.*ApplyVisualSettings"
---

<objective>
Ajouter les contrôles de personnalisation visuelle dans la section TwitchChat de MainWindow et valider la fonctionnalité complète via checkpoint humain.

Purpose: L'utilisateur peut choisir une image de bandeau, masquer le bandeau, et personnaliser les couleurs de l'overlay — tout en voyant les changements appliqués en temps réel.
Output: Section TwitchChat de MainWindow enrichie + checkpoint de validation humaine.
</objective>

<execution_context>
@C:/Users/Romain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Romain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-twitch-chat-visual-customization-image-header-et-color-picker-pour-l-overlay-chat/04-CONTEXT.md
@.planning/phases/04-twitch-chat-visual-customization-image-header-et-color-picker-pour-l-overlay-chat/04-RESEARCH.md
@.planning/phases/04-twitch-chat-visual-customization-image-header-et-color-picker-pour-l-overlay-chat/04-02-SUMMARY.md

<interfaces>
<!-- Section TwitchChat actuelle dans MainWindow.xaml.cs (lignes 452-534) -->
<!-- Le code existant se termine par : -->
```csharp
if (key == "TwitchChat")
{
    AddSep();
    // [section CANAL TWITCH avec channelRow et connectBtn]
    Add(channelRow);
    AddSep();
    // [section Max messages avec AddSlider]
    AddSlider("Nb messages", _config.Twitch.MaxMessages, 5, 50,
        v => _config.Twitch.MaxMessages = (int)v, "F0");
}
// ← AJOUTER les nouvelles sections ICI, avant la fermeture du bloc
```

<!-- AddColorPicker existant (ligne 670) — à RÉUTILISER avec de nouveaux presets -->
```csharp
private void AddColorPicker(string label, string currentHex, Action<string> set)
// Affiche 8 swatches colorés. set(hex) appelé au clic — NE SAUVEGARDE PAS lui-même.
// Le caller DOIT appeler _configService.Save() dans la lambda set.
```

<!-- AddToggle existant (ligne 658) -->
```csharp
private void AddToggle(string label, bool val, Action<bool> set)
```

<!-- Helper Add (ligne 638 environ) -->
```csharp
private void Add(UIElement e) { ... }
private void AddSep() { ... }
```

<!-- GetOverlay hot-update pattern (MainWindow.xaml.cs ligne ~335) -->
```csharp
_overlayManager.GetOverlay<BlindSpotOverlay>("BlindSpot")?.ApplySettings();
// Pattern pour TwitchChat :
_overlayManager.GetOverlay<TwitchChatOverlay>("TwitchChat")?.ApplyVisualSettings();
```

<!-- InputDisplayConfig.ParseColor — parse 6-digit hex RGB uniquement -->
```csharp
public static Color ParseColor(string hex)  // pour les swatches 6-digit du AddColorPicker existant
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter les contrôles visuels dans la section TwitchChat de MainWindow</name>
  <files>LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay/Views/MainWindow.xaml.cs</files>
  <action>
    Localiser le bloc `if (key == "TwitchChat")` dans MainWindow.xaml.cs (ligne ~452).
    À la fin du bloc existant (après le AddSlider "Nb messages"), ajouter les nouvelles sections ci-dessous.
    Ajouter l'using `using LMUOverlay.Views.Overlays;` si absent (pour TwitchChatOverlay).

    SECTION 1 — IMAGE BANDEAU :
    ```csharp
    AddSep();
    Add(new TextBlock
    {
        Text       = "IMAGE BANDEAU",
        FontSize   = 9,
        FontFamily = new FontFamily("Segoe UI"),
        FontWeight = FontWeights.Bold,
        Foreground = new SolidColorBrush(Color.FromRgb(145, 70, 255)),
        Margin     = new Thickness(0, 0, 0, 6),
    });

    // Ligne : label (60px) | chemin affiché (stretch) | bouton PARCOURIR (Auto)
    var imageRow = new Grid { Margin = new Thickness(0, 0, 0, 4) };
    imageRow.ColumnDefinitions.Add(new ColumnDefinition { Width = new GridLength(60) });
    imageRow.ColumnDefinitions.Add(new ColumnDefinition());
    imageRow.ColumnDefinitions.Add(new ColumnDefinition { Width = GridLength.Auto });

    imageRow.Children.Add(new TextBlock
    {
        Text              = "Image",
        FontSize          = 11,
        FontFamily        = new FontFamily("Segoe UI"),
        Foreground        = B(163, 163, 163),
        VerticalAlignment = VerticalAlignment.Center,
    });

    var pathDisplay = new TextBlock
    {
        Text              = string.IsNullOrEmpty(_config.Twitch.HeaderImagePath)
                            ? "(aucune)"
                            : System.IO.Path.GetFileName(_config.Twitch.HeaderImagePath),
        FontSize          = 10,
        FontFamily        = new FontFamily("Consolas"),
        Foreground        = B(34, 197, 94),
        VerticalAlignment = VerticalAlignment.Center,
        Margin            = new Thickness(4, 0, 4, 0),
        TextTrimming      = TextTrimming.CharacterEllipsis,
    };
    Grid.SetColumn(pathDisplay, 1);
    imageRow.Children.Add(pathDisplay);

    var browseBtn = new Button
    {
        Content    = "PARCOURIR",
        FontSize   = 9,
        Padding    = new Thickness(10, 3, 10, 3),
        Style      = (Style)FindResource("FlatToggle"),
        Foreground = new SolidColorBrush(Color.FromRgb(145, 70, 255)),
    };
    Grid.SetColumn(browseBtn, 2);
    imageRow.Children.Add(browseBtn);

    browseBtn.Click += (_, _) =>
    {
        var dlg = new Microsoft.Win32.OpenFileDialog
        {
            Filter = "Images|*.png;*.jpg;*.jpeg;*.bmp",
            Title  = "Choisir une image de bandeau",
        };
        if (dlg.ShowDialog() == true)
        {
            _config.Twitch.HeaderImagePath = dlg.FileName;
            pathDisplay.Text = System.IO.Path.GetFileName(dlg.FileName);
            _configService.Save(_config);
            _overlayManager.GetOverlay<TwitchChatOverlay>("TwitchChat")?.ApplyVisualSettings();
        }
    };

    Add(imageRow);

    // Bouton pour effacer l'image
    var clearImageBtn = new Button
    {
        Content    = "Effacer l'image",
        FontSize   = 9,
        Padding    = new Thickness(10, 3, 10, 3),
        Style      = (Style)FindResource("FlatToggle"),
        Foreground = B(163, 163, 163),
        Margin     = new Thickness(0, 2, 0, 0),
    };
    clearImageBtn.Click += (_, _) =>
    {
        _config.Twitch.HeaderImagePath = "";
        pathDisplay.Text = "(aucune)";
        _configService.Save(_config);
        _overlayManager.GetOverlay<TwitchChatOverlay>("TwitchChat")?.ApplyVisualSettings();
    };
    Add(clearImageBtn);
    ```

    SECTION 2 — TOGGLE MASQUER LE BANDEAU :
    ```csharp
    AddSep();
    AddToggle("Masquer le bandeau", !_config.Twitch.ShowHeader, v =>
    {
        _config.Twitch.ShowHeader = !v;  // toggle inversé : checked = masqué
        _configService.Save(_config);
        _overlayManager.GetOverlay<TwitchChatOverlay>("TwitchChat")?.ApplyVisualSettings();
    });
    ```

    SECTION 3 — COULEURS :
    ```csharp
    AddSep();
    Add(new TextBlock
    {
        Text       = "COULEURS",
        FontSize   = 9,
        FontFamily = new FontFamily("Segoe UI"),
        FontWeight = FontWeights.Bold,
        Foreground = new SolidColorBrush(Color.FromRgb(145, 70, 255)),
        Margin     = new Thickness(0, 0, 0, 6),
    });

    // Presets Twitch pour couleur de fond (6-digit RGB, alpha appliqué à l'affichage)
    string[] bgPresets = { "#0C0E12", "#1A1A2E", "#16213E", "#0F3460", "#1E1F2E", "#2D2D44", "#3D1A4A", "#000000" };
    // Presets Twitch pour couleur accent
    string[] accPresets = { "#9146FF", "#6441A5", "#4CD964", "#007AFF", "#FF3B30", "#FF9500", "#FFCC00", "#FFFFFF" };

    // Color picker FOND
    AddColorPicker("Fond", 
        string.IsNullOrEmpty(_config.Twitch.BackgroundColor) ? "#0C0E12" : _config.Twitch.BackgroundColor,
        v =>
        {
            _config.Twitch.BackgroundColor = v;
            _configService.Save(_config);
            _overlayManager.GetOverlay<TwitchChatOverlay>("TwitchChat")?.ApplyVisualSettings();
        });
    
    var resetBgBtn = new Button
    {
        Content    = "Reset fond",
        FontSize   = 9,
        Padding    = new Thickness(10, 3, 10, 3),
        Style      = (Style)FindResource("FlatToggle"),
        Foreground = B(163, 163, 163),
        Margin     = new Thickness(0, 2, 0, 6),
    };
    resetBgBtn.Click += (_, _) =>
    {
        _config.Twitch.BackgroundColor = "";
        _configService.Save(_config);
        _overlayManager.GetOverlay<TwitchChatOverlay>("TwitchChat")?.ApplyVisualSettings();
    };
    Add(resetBgBtn);

    // Color picker ACCENT
    AddColorPicker("Accent",
        string.IsNullOrEmpty(_config.Twitch.AccentColor) ? "#9146FF" : _config.Twitch.AccentColor,
        v =>
        {
            _config.Twitch.AccentColor = v;
            _configService.Save(_config);
            _overlayManager.GetOverlay<TwitchChatOverlay>("TwitchChat")?.ApplyVisualSettings();
        });

    var resetAccBtn = new Button
    {
        Content    = "Reset accent",
        FontSize   = 9,
        Padding    = new Thickness(10, 3, 10, 3),
        Style      = (Style)FindResource("FlatToggle"),
        Foreground = B(163, 163, 163),
        Margin     = new Thickness(0, 2, 0, 0),
    };
    resetAccBtn.Click += (_, _) =>
    {
        _config.Twitch.AccentColor = "";
        _configService.Save(_config);
        _overlayManager.GetOverlay<TwitchChatOverlay>("TwitchChat")?.ApplyVisualSettings();
    };
    Add(resetAccBtn);
    ```

    RÈGLES :
    - Conserver le pattern code-behind pur (pas XAML/MVVM)
    - Chaque callback : (1) mettre à jour _config.Twitch.X, (2) _configService.Save(_config), (3) ApplyVisualSettings()
    - Ne pas toucher à la section CANAL TWITCH ni au AddSlider "Nb messages" existants
    - AddColorPicker existant utilise ses propres presets internes — on peut passer n'importe quel currentHex
    - Utiliser B(r,g,b) pour les couleurs internes (helper déjà disponible dans MainWindow)
  </action>
  <verify>
    <automated>dotnet build LMUOverlay\LMUOverlay\LMUOverlay\LMUOverlay\LMUOverlay.csproj --no-restore 2>&1 | tail -5</automated>
    Build succeeded. 0 Error(s). Aucune régression : dotnet test --no-build passe la suite complète.
  </verify>
  <done>
    MainWindow.xaml.cs compile. La section TwitchChat contient : bouton PARCOURIR, Effacer l'image, toggle Masquer le bandeau, AddColorPicker Fond + Reset fond, AddColorPicker Accent + Reset accent. Build 0 errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Personnalisation visuelle complète de TwitchChatOverlay :
    - Bouton PARCOURIR → OpenFileDialog PNG/JPG/BMP → image dans le bandeau
    - Bouton Effacer l'image → retour au texte "TCHAT"
    - Toggle "Masquer le bandeau" → cache header + séparateur entièrement
    - Color picker Fond (8 swatches) + bouton Reset fond
    - Color picker Accent (8 swatches) + bouton Reset accent
    - Tous les changements appliqués live sur l'overlay sans redémarrage
    - Persistance dans config.json
  </what-built>
  <how-to-verify>
    Prérequis : Lancer l'application, activer l'overlay TwitchChat, ouvrir ses settings dans MainWindow.

    1. **Image picker (TWITCH-V-03)**
       - Cliquer "PARCOURIR" → vérifier qu'OpenFileDialog s'ouvre avec filtre Images (PNG/JPG/BMP)
       - Sélectionner une image PNG → le bandeau de l'overlay doit afficher l'image à la place du texte "TCHAT"
       - Vérifier que le nom de fichier s'affiche dans pathDisplay
    
    2. **Masquer le bandeau (TWITCH-V-04)**
       - Cocher "Masquer le bandeau" → header ET séparateur doivent disparaître de l'overlay
       - Décocher → header et séparateur réapparaissent avec le bon contenu
    
    3. **Couleurs live (TWITCH-V-05)**
       - Cliquer un swatch de fond → le fond de l'overlay change immédiatement (sans redémarrage)
       - Cliquer un swatch accent → la barre séparateur change de couleur immédiatement
    
    4. **Reset (TWITCH-V-06)**
       - Cliquer "Reset fond" → le fond revient à la couleur PanelBackground du thème actuel
       - Cliquer "Reset accent" → le séparateur revient au violet Twitch (#9146FF alpha ~31%)
    
    5. **Persistance** : Fermer et relancer l'app → les couleurs et l'image choisies sont préservées
    
    6. **Fallback image manquante (TWITCH-V-07)** : Supprimer/déplacer le fichier image sélectionné, relancer l'app → overlay affiche "TCHAT" (texte fallback), aucun crash
  </how-to-verify>
  <resume-signal>Taper "approuvé" si tout fonctionne, ou décrire les problèmes observés</resume-signal>
</task>

</tasks>

<verification>
dotnet test LMUOverlay\LMUOverlay\LMUOverlay\LMUOverlay.Tests\ --no-build
Full suite green (FuelStrategy + UI + WebBrowser + TwitchVisual) + checkpoint humain approuvé.
</verification>

<success_criteria>
- Build 0 errors, full test suite green
- Bouton PARCOURIR fonctionnel avec filtre PNG/JPG/BMP
- Image affichée dans le bandeau de l'overlay après sélection
- Toggle Masquer le bandeau fonctionne sur header + séparateur
- Color pickers Fond et Accent avec Reset fonctionnent live
- Persistance config.json confirmée après redémarrage
- Fallback "TCHAT" si image manquante au démarrage
- Checkpoint humain approuvé
</success_criteria>

<output>
After completion, create `.planning/phases/04-twitch-chat-visual-customization-image-header-et-color-picker-pour-l-overlay-chat/04-03-SUMMARY.md`
</output>
