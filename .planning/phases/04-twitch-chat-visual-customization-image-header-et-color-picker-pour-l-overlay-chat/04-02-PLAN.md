---
phase: 04-twitch-chat-visual-customization-image-header-et-color-picker-pour-l-overlay-chat
plan: 02
type: execute
wave: 1
depends_on:
  - "04-01"
files_modified:
  - LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay/Models/OverlayConfig.cs
  - LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay/Views/Overlays/TwitchChatOverlay.cs
autonomous: true
requirements:
  - TWITCH-V-01
  - TWITCH-V-02

must_haves:
  truths:
    - "TwitchSettings a 4 nouveaux champs avec valeurs par défaut C# sentinel"
    - "Un vieux config.json sans ces champs se désérialise sans erreur vers les défauts"
    - "TwitchChatOverlay stocke des références aux éléments visuels mutables"
    - "TwitchChatOverlay expose ApplyVisualSettings() appelable depuis l'extérieur"
    - "Les tests TWITCH-V-01 et TWITCH-V-02 passent (GREEN)"
  artifacts:
    - path: "LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay/Models/OverlayConfig.cs"
      provides: "TwitchSettings avec HeaderImagePath, ShowHeader, BackgroundColor, AccentColor"
      contains: "HeaderImagePath"
    - path: "LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay/Views/Overlays/TwitchChatOverlay.cs"
      provides: "ApplyVisualSettings() + champs de référence _outerBorder, _sepBorder, _headerGrid"
      exports: ["ApplyVisualSettings"]
  key_links:
    - from: "TwitchChatOverlay.ApplyVisualSettings()"
      to: "TwitchSettings (BackgroundColor, AccentColor, HeaderImagePath, ShowHeader)"
      via: "_config.Twitch lecture + BrushCache.Get() + ThemeManager.ParseColor()"
      pattern: "_config\\.Twitch\\.(BackgroundColor|AccentColor)"
    - from: "TwitchVisualConfigTests"
      to: "TwitchSettings"
      via: "accès direct aux propriétés + JsonConvert"
      pattern: "config\\.Twitch\\.HeaderImagePath"
---

<objective>
Étendre le modèle TwitchSettings et refactoriser TwitchChatOverlay pour supporter la personnalisation visuelle.

Purpose: Fournir la fondation de données (modèle + persistance) et la capacité de mise à jour à chaud (ApplyVisualSettings) avant d'ajouter les contrôles UI dans le Plan 04-03.
Output: TwitchSettings avec 4 champs + TwitchChatOverlay avec ApplyVisualSettings() + tests GREEN.
</objective>

<execution_context>
@C:/Users/Romain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Romain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-twitch-chat-visual-customization-image-header-et-color-picker-pour-l-overlay-chat/04-CONTEXT.md
@.planning/phases/04-twitch-chat-visual-customization-image-header-et-color-picker-pour-l-overlay-chat/04-RESEARCH.md
@.planning/phases/04-twitch-chat-visual-customization-image-header-et-color-picker-pour-l-overlay-chat/04-01-SUMMARY.md

<interfaces>
<!-- TwitchSettings actuel (OverlayConfig.cs lignes 71-75) -->
```csharp
public class TwitchSettings
{
    public string Channel     { get; set; } = "";
    public int    MaxMessages { get; set; } = 20;
}
```

<!-- TwitchChatOverlay.cs actuel — structure de la grille racine -->
```csharp
// root = Grid à 3 lignes : header(Auto) + sep(Auto) + messages(*)
var outer = new Border { Background = BrushCache.Get(Color.FromArgb(200, ...)), ... };
var root  = new Grid();
// row 0: header Grid (TextBlock "TWITCH" + _statusText)
var header = new Grid { ... };
header.Children.Add(new TextBlock { Text = "TWITCH", ... });  // à remplacer par Image ou TextBlock conditionnel
// row 1: sep Border avec Color.FromArgb(80, 145, 70, 255)
var sep = new Border { Height = 1, Background = new SolidColorBrush(Color.FromArgb(80, 145, 70, 255)), ... };
// row 2: ScrollViewer + _messagePanel
outer.Child = root;
Content = outer;
```

<!-- BrushCache.Get — frozen brush pattern -->
```csharp
BrushCache.Get(color)  // retourne SolidColorBrush frozen — NE PAS modifier, toujours affecter un nouveau brush
```

<!-- ThemeManager.ParseColor — ARGB hex parsing -->
```csharp
public static Color ParseColor(string hex)  // gère #RRGGBB et #AARRGGBB
```

<!-- Pattern sentinel vide (recherche de référence : InputGraphConfig.ThrottleColor) -->
```csharp
public string ThrottleColor { get; set; } = "#4CD964";
// Twitch : "" = utiliser la valeur par défaut du thème
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Étendre TwitchSettings dans OverlayConfig.cs (GREEN les tests RED)</name>
  <files>LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay/Models/OverlayConfig.cs</files>
  <behavior>
    - Test TWITCH-V-01: HeaderImagePath, ShowHeader, BackgroundColor, AccentColor survivent au round-trip JSON
    - Test TWITCH-V-02: Valeurs par défaut correctes quand les champs sont absents du JSON
  </behavior>
  <action>
    Ajouter 4 propriétés à la classe TwitchSettings dans OverlayConfig.cs (après MaxMessages, avant la fin de la classe) :

    ```csharp
    // Nouveaux champs Phase 4 — personnalisation visuelle
    public string HeaderImagePath { get; set; } = "";   // "" = aucune image (fallback texte "TCHAT")
    public bool   ShowHeader      { get; set; } = true; // false = masque header + séparateur
    public string BackgroundColor { get; set; } = "";   // "" = utilise PanelBackground du thème (alpha 200)
    public string AccentColor     { get; set; } = "";   // "" = utilise Twitch purple #9146FF (alpha 80)
    ```

    Règles importantes :
    - Valeur sentinel "" pour les couleurs : l'overlay interprète "" comme "utiliser le défaut du thème"
    - ShowHeader défaut true : comportement identique à l'existant pour les configs sans ce champ
    - Newtonsoft.Json désérialise les clés absentes vers la valeur initiale C# — aucune migration nécessaire
    - NE PAS toucher aux autres classes dans OverlayConfig.cs
  </action>
  <verify>
    <automated>dotnet test LMUOverlay\LMUOverlay\LMUOverlay\LMUOverlay.Tests\ --filter "Category=TwitchVisual" --no-build</automated>
    Les 2 tests doivent passer (GREEN). Si le build échoue, vérifier les usings manquants dans le test.
  </verify>
  <done>
    TwitchSettings a 4 nouveaux champs. dotnet test --filter Category=TwitchVisual retourne 2/2 passed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactoriser TwitchChatOverlay — champs de référence + ApplyVisualSettings()</name>
  <files>LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay/Views/Overlays/TwitchChatOverlay.cs</files>
  <action>
    Modifier TwitchChatOverlay.cs pour stocker des références aux éléments visuels mutables et exposer ApplyVisualSettings().

    ÉTAPE 1 — Ajouter les champs de référence (après les champs existants _statusText) :
    ```csharp
    private Border     _outerBorder   = null!;
    private Border     _sepBorder     = null!;
    private Grid       _headerGrid    = null!;  // row 0 — le header
    private TextBlock? _headerText;
    private Image?     _headerImage;
    ```

    ÉTAPE 2 — Dans le constructeur, stocker les références ET ajouter Image conditionnelle dans le header Grid :

    Après `var outer = new Border { ... }` :
    - Assigner `_outerBorder = outer;`

    Après `var sep = new Border { ... }` :
    - Assigner `_sepBorder = sep;`

    Remplacer le TextBlock "TWITCH" statique par un Grid conditionnel dans le header row 0.
    Le header Grid (col 0 = stretch, col 1 = Auto pour _statusText) doit contenir :
    - `_headerText` = TextBlock { Text="TCHAT", mêmes styles que l'actuel "TWITCH" } (visible par défaut)
    - `_headerImage` = Image { Stretch=Uniform, MaxHeight=18, Visibility=Collapsed } (invisible par défaut)
    
    Assigner `_headerGrid = header;`

    ÉTAPE 3 — Appeler `ApplyVisualSettings()` à la fin du constructeur (avant la connexion aux events).

    ÉTAPE 4 — Implémenter `public void ApplyVisualSettings()` :
    ```csharp
    public void ApplyVisualSettings()
    {
        var tw = _config.Twitch;

        // Couleur de fond (alpha 200 fixe)
        Color bg = string.IsNullOrEmpty(tw.BackgroundColor)
            ? Color.FromArgb(200, ThemeManager.Current.PanelBackground.R,
                                  ThemeManager.Current.PanelBackground.G,
                                  ThemeManager.Current.PanelBackground.B)
            : Color.FromArgb(200, ThemeManager.ParseColor(tw.BackgroundColor).R,
                                  ThemeManager.ParseColor(tw.BackgroundColor).G,
                                  ThemeManager.ParseColor(tw.BackgroundColor).B);
        _outerBorder.Background = BrushCache.Get(bg);

        // Couleur accent (alpha 80 fixe)
        Color accent = string.IsNullOrEmpty(tw.AccentColor)
            ? Color.FromArgb(80, 145, 70, 255)
            : Color.FromArgb(80, ThemeManager.ParseColor(tw.AccentColor).R,
                                 ThemeManager.ParseColor(tw.AccentColor).G,
                                 ThemeManager.ParseColor(tw.AccentColor).B);
        _sepBorder.Background = BrushCache.Get(accent);

        // Visibilité du header
        _headerGrid.Visibility = tw.ShowHeader ? Visibility.Visible : Visibility.Collapsed;
        _sepBorder.Visibility  = tw.ShowHeader ? Visibility.Visible : Visibility.Collapsed;

        // Contenu du header : image vs texte
        if (!string.IsNullOrEmpty(tw.HeaderImagePath) && File.Exists(tw.HeaderImagePath))
        {
            var bmp = new BitmapImage();
            bmp.BeginInit();
            bmp.UriSource   = new Uri(tw.HeaderImagePath, UriKind.Absolute);
            bmp.CacheOption = BitmapCacheOption.OnLoad;  // ferme le handle fichier immédiatement
            bmp.EndInit();
            _headerImage!.Source     = bmp;
            _headerImage.Visibility  = Visibility.Visible;
            _headerText!.Visibility  = Visibility.Collapsed;
        }
        else
        {
            _headerImage!.Visibility = Visibility.Collapsed;
            _headerText!.Visibility  = Visibility.Visible;
        }
    }
    ```

    AJOUTER l'using manquant en haut du fichier si absent :
    - `using System.IO;` (pour File.Exists)
    - `using System.Windows.Media.Imaging;` (pour BitmapImage)

    Anti-patterns à éviter :
    - NE PAS modifier le brush existant : toujours BrushCache.Get(newColor) puis affectation
    - NE PAS utiliser UriKind.Relative pour le chemin de l'image
    - NE PAS laisser le handle fichier ouvert : BitmapCacheOption.OnLoad obligatoire
    - NE PAS supprimer UpdateData() ou OnClosed() existants

    Vérifier que le projet compile sans avertissement supplémentaire.
  </action>
  <verify>
    <automated>dotnet build LMUOverlay\LMUOverlay\LMUOverlay\LMUOverlay\LMUOverlay.csproj --no-restore 2>&1 | tail -5</automated>
    Build succeeded. 0 Error(s). Tests TwitchVisual toujours GREEN après ce changement.
  </verify>
  <done>
    TwitchChatOverlay.cs compile. ApplyVisualSettings() public existe. _outerBorder, _sepBorder, _headerGrid, _headerText, _headerImage sont des champs d'instance. Build succeeds 0 errors.
  </done>
</task>

</tasks>

<verification>
dotnet test LMUOverlay\LMUOverlay\LMUOverlay\LMUOverlay.Tests\ --no-build
Full suite green — les 2 tests TwitchVisual passent, aucune régression sur les suites existantes (FuelStrategy, UI, WebBrowser).
</verification>

<success_criteria>
- TwitchSettings.HeaderImagePath, ShowHeader, BackgroundColor, AccentColor existent avec valeurs par défaut correctes
- TwitchChatOverlay.ApplyVisualSettings() est public et fonctionnel
- 2/2 tests TwitchVisual GREEN
- Full test suite : 0 failed
- Build 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-twitch-chat-visual-customization-image-header-et-color-picker-pour-l-overlay-chat/04-02-SUMMARY.md`
</output>
