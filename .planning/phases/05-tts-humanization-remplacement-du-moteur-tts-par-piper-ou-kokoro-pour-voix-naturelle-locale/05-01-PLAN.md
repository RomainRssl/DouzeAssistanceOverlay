---
phase: 05-tts-humanization-remplacement-du-moteur-tts-par-piper-ou-kokoro-pour-voix-naturelle-locale
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/PiperTTS/AlertTextsTests.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "AlertTextsTests.cs exists and compiles cleanly against the current codebase"
    - "All 6 test stubs fail (RED) with Assert.Fail() until Plan 05-02 adds the model"
    - "Tests are reachable via dotnet test --filter Category=PiperTTS"
  artifacts:
    - path: "LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/PiperTTS/AlertTextsTests.cs"
      provides: "TDD RED stubs: JSON round-trip, legacy migration, GetAlertText, 23 defaults"
  key_links:
    - from: "AlertTextsTests.cs"
      to: "LMUOverlay.Models.GeneralSettings"
      via: "using LMUOverlay.Models; AppConfig instantiation"
      pattern: "new AppConfig\\(\\)"
---

<objective>
Create RED TDD stubs for the Piper TTS AlertTexts feature — 6 failing tests that will turn GREEN when Plan 05-02 adds `AlertTexts` + `GetAlertText()` to `GeneralSettings`.

Purpose: Establish the test contract before implementation (mirrors TwitchVisualConfigTests approach from Phase 4).
Output: `LMUOverlay.Tests/PiperTTS/AlertTextsTests.cs` — 6 Assert.Fail() stubs, compilable, all RED.
</objective>

<execution_context>
@C:/Users/Romain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Romain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tts-humanization-remplacement-du-moteur-tts-par-piper-ou-kokoro-pour-voix-naturelle-locale/05-CONTEXT.md
@.planning/phases/05-tts-humanization-remplacement-du-moteur-tts-par-piper-ou-kokoro-pour-voix-naturelle-locale/05-RESEARCH.md

<interfaces>
<!-- Key contracts from LMUOverlay.Tests (existing test patterns) -->
<!-- Pattern from TwitchVisual\TwitchVisualConfigTests.cs — use this EXACT structure -->

Namespace convention:
  LMUOverlay.Tests.PiperTTS

Trait attribute:
  [Trait("Category", "PiperTTS")]

Usings required:
  using Xunit;
  using LMUOverlay.Models;
  using Newtonsoft.Json;

AppConfig root object:
  var config = new AppConfig();
  config.General.AlertTexts  // Dictionary<string, string> — added by Plan 05-02
  config.General.GetAlertText(key, defaultText)  // helper method — added by Plan 05-02

EnsureDefaultAlertTexts target (23 keys):
  BlueFlagWarning, RedFlag, GreenFlag, CheckeredFlag, YellowFlag,
  YellowFlagPitClosed, YellowFlagResume, FuelWindowOpen, FuelLowLaps,
  FuelCritical, GapCloseBehind, GapLostAhead, PositionGained, PositionLost,
  SpotterClear, Spotter_1, Spotter_2, Spotter_3, SectorBeat_S1, SectorBeat_S2,
  SectorBeat_S3, NewPersonalBest, __test__

RED stub pattern (from TwitchVisualConfigTests):
  [Fact(DisplayName = "...")]
  public void MethodName_Condition_ExpectedResult()
  {
      Assert.Fail("RED — implement in Plan 05-02");
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AlertTextsTests.cs with 6 RED stubs</name>
  <files>LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/PiperTTS/AlertTextsTests.cs</files>
  <action>
Create the directory `LMUOverlay.Tests/PiperTTS/` and write `AlertTextsTests.cs` containing 6 test classes with one `[Fact]` each, all using `Assert.Fail("RED — implement in Plan 05-02")`.

The 6 test stubs to create:

**Class 1: `AlertTextsRoundTripTests`** — `AlertTexts_Dictionary_SurvivesJsonRoundTrip`
Tests that `GeneralSettings.AlertTexts` (a `Dictionary<string,string>`) serializes and deserializes correctly via `JsonConvert`.

**Class 2: `AlertTextsLegacyConfigTests`** — `GeneralSettings_OldJsonWithoutAlertTexts_DeserializesToEmptyDict`
Tests that an old `config.json` (no `AlertTexts` key) deserializes without NullReferenceException — `AlertTexts` must be non-null with 0 entries.

**Class 3: `GetAlertTextFromDictTests`** — `GetAlertText_KeyPresentInDict_ReturnsStoredValue`
Tests that `GetAlertText("BlueFlagWarning", "default")` returns the stored value when the key exists in `AlertTexts`.

**Class 4: `GetAlertTextFallbackTests`** — `GetAlertText_KeyAbsentFromDict_ReturnsFallback`
Tests that `GetAlertText("MISSING_KEY", "fallback text")` returns `"fallback text"` when the key is absent.

**Class 5: `GetAlertTextEmptyStringTests`** — `GetAlertText_EmptyStringValue_ReturnsFallback`
Tests that `GetAlertText("BlueFlagWarning", "fallback")` returns `"fallback"` when the stored value is empty/whitespace (guards against accidental blank text).

**Class 6: `DefaultAlertTexts23KeysTests`** — `EnsureDefaultAlertTexts_PopulatesAll23Keys`
Tests that after calling `VoiceService.EnsureDefaultAlertTexts(settings.General)`, `settings.General.AlertTexts.Count == 23`.

File structure:
```
using Xunit;
using LMUOverlay.Models;
using LMUOverlay.Services;
using Newtonsoft.Json;

namespace LMUOverlay.Tests.PiperTTS
{
    [Trait("Category", "PiperTTS")]
    public class AlertTextsRoundTripTests { ... }

    [Trait("Category", "PiperTTS")]
    public class AlertTextsLegacyConfigTests { ... }

    [Trait("Category", "PiperTTS")]
    public class GetAlertTextFromDictTests { ... }

    [Trait("Category", "PiperTTS")]
    public class GetAlertTextFallbackTests { ... }

    [Trait("Category", "PiperTTS")]
    public class GetAlertTextEmptyStringTests { ... }

    [Trait("Category", "PiperTTS")]
    public class DefaultAlertTexts23KeysTests { ... }
}
```

IMPORTANT:
- `using LMUOverlay.Services;` is needed for `VoiceService.EnsureDefaultAlertTexts` (Class 6) — this is a static method that Plan 05-02 will add to VoiceService
- All 6 Fact methods must contain only `Assert.Fail("RED — implement in Plan 05-02");`
- The file must compile against the CURRENT codebase (before Plan 05-02 adds the new members) — since `AlertTexts` and `GetAlertText` don't exist yet, all references to them must be inside `Assert.Fail()` stub bodies only; the method signatures must not reference them in parameter types or return types
- Use `config.General.AlertTexts` (not `config.GeneralSettings`) — check `AppConfig` structure: the field name for `GeneralSettings` inside `AppConfig` is `General`
  </action>
  <verify>
    <automated>cd F:/CLAUDE/OVERLAY_LMU_2.0/LMUOverlay/LMUOverlay/LMUOverlay && dotnet test --filter "Category=PiperTTS" -v minimal 2>&1 | tail -20</automated>
  </verify>
  <done>
    - File exists at `LMUOverlay.Tests/PiperTTS/AlertTextsTests.cs`
    - `dotnet test --filter "Category=PiperTTS"` shows 6 tests FAILED (Assert.Fail RED — not compile errors)
    - `dotnet build` succeeds with 0 errors
  </done>
</task>

</tasks>

<verification>
Run after task completes:
1. `dotnet test --filter "Category=PiperTTS" -v minimal` — must show 6 FAILED (not 0 tests found, not compile errors)
2. `dotnet build` — must succeed with 0 errors
3. `dotnet test` (full suite) — all previously-passing tests still pass; only the 6 new PiperTTS stubs are RED
</verification>

<success_criteria>
- `PiperTTS/AlertTextsTests.cs` exists and compiles
- 6 tests visible under `Category=PiperTTS`
- All 6 are RED (Assert.Fail)
- Zero regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/05-tts-humanization-remplacement-du-moteur-tts-par-piper-ou-kokoro-pour-voix-naturelle-locale/05-01-SUMMARY.md`
</output>
