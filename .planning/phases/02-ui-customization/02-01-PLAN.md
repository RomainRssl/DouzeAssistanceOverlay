---
phase: 02-ui-customization
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/UI/SnapGridTests.cs
  - LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/UI/VrProfileTests.cs
  - LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/UI/ThemePresetTests.cs
  - LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/UI/ColorOverrideTests.cs
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "dotnet test --filter Category=UI exits 0 with 0 failures (all tests pass or are skipped)"
    - "4 test files exist under LMUOverlay.Tests/UI/"
    - "Tests reference pure static logic — no WPF window instantiation"
  artifacts:
    - path: "LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/UI/SnapGridTests.cs"
      provides: "Snap-to-grid math tests (UI-01)"
      contains: "[Trait(\"Category\", \"UI\")]"
    - path: "LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/UI/VrProfileTests.cs"
      provides: "VR profile copy logic tests (UI-04)"
      contains: "[Trait(\"Category\", \"UI\")]"
    - path: "LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/UI/ThemePresetTests.cs"
      provides: "Preset theme JSON write tests (UI-03)"
      contains: "[Trait(\"Category\", \"UI\")]"
    - path: "LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/UI/ColorOverrideTests.cs"
      provides: "Per-overlay color override lookup tests (UI-01/02)"
      contains: "[Trait(\"Category\", \"UI\")]"
  key_links:
    - from: "LMUOverlay.Tests/UI/"
      to: "dotnet test --filter Category=UI"
      via: "[Trait(\"Category\", \"UI\")] on every test class"
      pattern: "Trait.*Category.*UI"
---

<objective>
Create the 4 xUnit test stub files for Phase 2. These files define the RED tests that Wave 1 implementation plans must make GREEN. No production code changes in this plan — only test scaffolding.

Purpose: Nyquist compliance — every Phase 2 automated verify command must reference a file that exists before implementation starts.
Output: 4 test files under LMUOverlay.Tests/UI/ covering snap math, VR profile copy, theme preset writing, and color override lookup.
</objective>

<execution_context>
@C:/Users/Romain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Romain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-customization/02-CONTEXT.md
@.planning/phases/02-ui-customization/02-RESEARCH.md
</context>

<interfaces>
<!-- Existing test infrastructure — executor must follow these patterns -->

From LMUOverlay.Tests.csproj:
- TargetFramework: net8.0-windows, UseWPF: false
- xUnit 2.9.0
- ProjectReference: ../LMUOverlay/LMUOverlay.csproj

Existing test pattern (from FuelStrategyCalculatorTests.cs):
```csharp
using Xunit;
namespace LMUOverlay.Tests.FuelStrategy
{
    [Trait("Category", "Fuel")]
    public class FuelStrategyCalculatorTests
    {
        [Fact(DisplayName = "...")]
        public void MethodName_Condition_ExpectedResult() { ... }
    }
}
```

From OverlayConfig.cs (Models):
```csharp
public class OverlaySettings : INotifyPropertyChanged
{
    public double PosX { ... }
    public double PosY { ... }
    public double OverlayWidth { ... }
    public double OverlayHeight { ... }
    public bool IsLocked { ... }
    public double Opacity { ... }
    public double BackgroundOpacity { ... }
    public Dictionary<string, object> CustomOptions { get; set; } = new();
    // VrPosX/VrPosY/VrWidth/VrHeight do NOT exist yet — tests reference them as static logic inputs
}
```

From ThemeManager.cs (Helpers):
```csharp
public static string ThemesDirectory => ...  // %AppData%/DouzeAssistance/themes/
public static void EnsureDefaultThemeExists()  // already exists, writes endurance-noir.json
// EnsurePresetThemesExist() does NOT exist yet — test verifies it will write 3 files
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SnapGridTests.cs</name>
  <files>LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/UI/SnapGridTests.cs</files>
  <behavior>
    - Test 1: Snap(0, 10) == 0 (already on grid)
    - Test 2: Snap(14.9, 10) == 10 (rounds down)
    - Test 3: Snap(15.0, 10) == 20 (rounds up at midpoint)
    - Test 4: Snap(23.4, 10) == 20 (rounds down mid-cell)
    - Test 5: Snap(-7.0, 10) == -10 (negative values)
    - Test 6: Snap(1920.0, 10) == 1920 (large value already on grid)
    - Test 7: Raw accumulation — simulate 3 MouseMove deltas, raw tracks 1.2+1.8+2.5=5.5px, Snap gives 10
  </behavior>
  <action>
    Create the file at the exact path. Namespace: LMUOverlay.Tests.UI. Class: SnapGridTests.
    Trait: [Trait("Category", "UI")].

    The tests call a static method SnapGridHelper.Snap(double rawValue, double grid) that does NOT exist yet.
    Use the full qualified name LMUOverlay.Helpers.SnapGridHelper.Snap(...) in test calls.
    Tests will be RED (compile error or fail) until Plan 02-02 creates SnapGridHelper.

    Write the tests exactly as described in the behavior block — no placeholders, no Skip attributes.
    The test file must compile once SnapGridHelper is added. Use `// TODO: implement in 02-02` comment
    above the first test method to signal what unblocks these tests.

    File structure:
    ```csharp
    using Xunit;
    using LMUOverlay.Helpers;

    namespace LMUOverlay.Tests.UI
    {
        [Trait("Category", "UI")]
        public class SnapGridTests
        {
            // TODO: implement SnapGridHelper.Snap in 02-02
            [Fact(DisplayName = "Snap: value already on grid boundary returns unchanged")]
            public void Snap_ValueOnGrid_ReturnsUnchanged() { ... }
            // ... remaining tests
        }
    }
    ```
  </action>
  <verify>
    <automated>dotnet build "LMUOverlay\LMUOverlay\LMUOverlay\LMUOverlay.Tests\LMUOverlay.Tests.csproj" 2>&1 | tail -5</automated>
  </verify>
  <done>File exists at the path, class decorated [Trait("Category","UI")], 7 test methods present. Build may fail on missing SnapGridHelper — that is expected (RED state).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create VrProfileTests.cs, ThemePresetTests.cs, ColorOverrideTests.cs</name>
  <files>
    LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/UI/VrProfileTests.cs,
    LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/UI/ThemePresetTests.cs,
    LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay.Tests/UI/ColorOverrideTests.cs
  </files>
  <behavior>
    VrProfileTests (namespace LMUOverlay.Tests.UI, [Trait("Category","UI")]):
    - Test 1: ApplyVrProfile_WhenVrFieldsNull_CopiesFrom2DFields
      Input: OverlaySettings with PosX=100, PosY=200, OverlayWidth=300, OverlayHeight=150, VrPosX=null
      Expected: after VrProfileHelper.InitVrFromTwoD(settings), VrPosX==100, VrPosY==200, VrWidth==300, VrHeight==150
    - Test 2: ApplyVrProfile_WhenVrFieldsSet_DoesNotOverwrite
      Input: VrPosX=50 already set
      Expected: VrPosX remains 50 after InitVrFromTwoD (only copies when null)
    - Test 3: SaveInVrMode_WritesToVrFields_Not2DFields
      Input: call VrProfileHelper.SaveDragResult(settings, newLeft=400, newTop=500, isVrActive=true)
      Expected: VrPosX==400, VrPosY==500, PosX unchanged at 100
    - Test 4: SaveInTwoDMode_WritesToPosXY_NotVrFields
      Input: call VrProfileHelper.SaveDragResult(settings, newLeft=400, newTop=500, isVrActive=false)
      Expected: PosX==400, PosY==500, VrPosX remains null

    ThemePresetTests (namespace LMUOverlay.Tests.UI, [Trait("Category","UI")]):
    - Test 1: EnsurePresetThemesExist_WritesRacingClair_WhenAbsent
      Use a temp directory, call ThemeManager.EnsurePresetThemesExistIn(tempDir), assert File.Exists("racing-clair.json")
    - Test 2: EnsurePresetThemesExist_WritesBleuNuitLmp2_WhenAbsent
      Same pattern, assert "bleu-nuit-lmp2.json"
    - Test 3: EnsurePresetThemesExist_WritesMinimalisteMono_WhenAbsent
      Same pattern, assert "minimaliste-mono.json"
    - Test 4: EnsurePresetThemesExist_DoesNotOverwrite_WhenFileExists
      Write a sentinel file "racing-clair.json" with content "CUSTOM", call EnsurePresetThemesExistIn, assert content is still "CUSTOM"

    ColorOverrideTests (namespace LMUOverlay.Tests.UI, [Trait("Category","UI")]):
    - Test 1: GetColorOverride_WhenKeyAbsent_ReturnsNull
      Input: OverlaySettings with empty CustomOptions
      Expected: ColorOverrideHelper.Get(settings, "ColorBg") == null
    - Test 2: GetColorOverride_WhenKeyPresent_ReturnsHexString
      Input: settings.CustomOptions["ColorBg"] = "#FF0000"
      Expected: ColorOverrideHelper.Get(settings, "ColorBg") == "#FF0000"
    - Test 3: SetColorOverride_StoresHexInCustomOptions
      Input: call ColorOverrideHelper.Set(settings, "ColorText", "#FFFFFF")
      Expected: settings.CustomOptions["ColorText"] == "#FFFFFF"
    - Test 4: SetColorOverride_Overwrite_UpdatesExistingKey
      Input: existing "ColorAccent"="#0000FF", call Set(..., "#00FF00")
      Expected: settings.CustomOptions["ColorAccent"] == "#00FF00"
  </behavior>
  <action>
    Create all 3 files. Each file follows the same namespace/trait pattern as SnapGridTests.

    Key decisions:
    - VrProfileTests references LMUOverlay.Helpers.VrProfileHelper (static helper, does NOT exist yet — created in 02-04).
      OverlaySettings in tests must be instantiated directly (no WPF). Use `new OverlaySettings { ... }`.
      VrPosX/VrPosY/VrWidth/VrHeight fields do NOT exist yet — tests will be RED until 02-04 adds them to OverlaySettings.

    - ThemePresetTests references ThemeManager.EnsurePresetThemesExistIn(string directory) overload
      (NOT the production EnsurePresetThemesExist() which uses the real %AppData% path).
      This overload does NOT exist yet — created in 02-03.
      Use System.IO.Path.GetTempPath() + Guid subdirectory for isolation.
      Dispose temp dirs in [IDisposable] or inline cleanup after each test.

    - ColorOverrideTests references LMUOverlay.Helpers.ColorOverrideHelper (static helper, created in 02-02).
      OverlaySettings instantiated directly.

    Add `// TODO: implement in 02-0X` comments above each class to indicate which plan unblocks them.
    Tests must NOT use [Skip] — they should be properly RED (compile/fail) to drive TDD.
  </action>
  <verify>
    <automated>dotnet build "LMUOverlay\LMUOverlay\LMUOverlay\LMUOverlay.Tests\LMUOverlay.Tests.csproj" 2>&1 | tail -10</automated>
  </verify>
  <done>3 files created. Each has [Trait("Category","UI")], correct namespace, and test methods as specified. Build may error on missing helpers/fields — expected (RED state). All 4 test files exist under LMUOverlay.Tests/UI/.</done>
</task>

</tasks>

<verification>
After both tasks:
- `dir LMUOverlay\LMUOverlay\LMUOverlay\LMUOverlay.Tests\UI\` shows 4 files
- Each file contains [Trait("Category", "UI")]
- Total test method count across 4 files: at least 19 (7+4+4+4)
- Build errors present only for missing helpers/fields (expected RED state)
</verification>

<success_criteria>
4 test stub files exist and define the RED tests that Wave 1 plans will make GREEN. No production code changed. Test infrastructure is Nyquist-compliant: every Phase 2 automated verify command has a corresponding test file.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-customization/02-01-SUMMARY.md`
</output>
