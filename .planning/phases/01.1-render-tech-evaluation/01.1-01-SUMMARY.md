---
phase: 01.1-render-tech-evaluation
plan: 01
subsystem: vr
tags: [openxr, wpf, rendertargetbitmap, stopwatch, performance, benchmarking]

# Dependency graph
requires: []
provides:
  - OpenXRService.UpdateAll() with cached RenderTargetBitmap (zero per-frame GC allocations when dimensions stable)
  - ProximityRadarOverlay.UpdateData() Stopwatch frame-time instrumentation logging min/avg/p95/max every 500 samples
affects:
  - 01.1-02 (SkiaSharp evaluation will compare against this WPF baseline)
  - 01.1-03 (D3D11 evaluation uses same baseline for comparison)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "RTB caching pattern: check CachedRtb == null || LastPixelW != w || LastPixelH != h before allocating"
    - "Frame-time instrumentation: Stopwatch.GetTimestamp() before/after render body, Queue<long>(500) rolling window, log stats every 500 samples"

key-files:
  created: []
  modified:
    - LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay/VR/OpenXRService.cs
    - LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay/Views/Overlays/ProximityRadarOverlay.cs

key-decisions:
  - "Match VROverlayService.CaptureAndSubmit() cached RTB pattern exactly — same fields (CachedRtb, CachedPixels, LastPixelW, LastPixelH) for consistency"
  - "Use _ticksPerUs = Frequency / 1_000_000L to convert ticks to microseconds for readable benchmark output"
  - "Rolling 500-sample window for stats (Queue with Dequeue when Count > 500) avoids unbounded memory growth"

patterns-established:
  - "RenderTargetBitmap caching: always guard allocation with dimension check, never allocate per frame"
  - "Benchmark instrumentation: GetTimestamp() pairs wrap entire render body, log on sample boundary"

requirements-completed: [VR-01]

# Metrics
duration: 2min
completed: 2026-05-19
---

# Phase 01.1 Plan 01: WPF Baseline Fix & Frame-Time Instrumentation Summary

**RTB allocation bug fixed in OpenXRService (cache per overlay, matching VROverlayService pattern) and ProximityRadarOverlay instrumented with Stopwatch microsecond frame-time logging every 500 samples**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-05-19T13:44:19Z
- **Completed:** 2026-05-19T13:45:59Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Eliminated per-frame RenderTargetBitmap allocation in OpenXRService.UpdateAll() — RTB now reused until overlay dimensions change, matching the VROverlayService.CaptureAndSubmit() pattern already in production
- Added XROvl.CachedRtb, CachedPixels, LastPixelW, LastPixelH fields to support the cache miss check
- ProximityRadarOverlay.UpdateData() now records Stopwatch microsecond timestamps before and after the entire Canvas children manipulation body
- Frame stats (min/avg/max/P95) logged to Debug output every 500 samples as `[ProximityRadar WPF baseline]` entries
- All 7 Category=Fuel xUnit tests remain green

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix OpenXRService.UpdateAll() allocation bug** - `a4d484b` (fix)
2. **Task 2: Instrument ProximityRadarOverlay with Stopwatch** - `c72277b` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay/VR/OpenXRService.cs` - XROvl class extended with cache fields; UpdateAll() rewritten to use cached RTB pattern
- `LMUOverlay/LMUOverlay/LMUOverlay/LMUOverlay/Views/Overlays/ProximityRadarOverlay.cs` - _renderTimes, _ticksPerUs, _sampleCount fields added; UpdateData() wrapped with GetTimestamp() timing + 500-sample stats logging

## Decisions Made

- Matched VROverlayService.CaptureAndSubmit() cached RTB pattern exactly (same field names: CachedRtb, CachedPixels, LastPixelW, LastPixelH) for codebase consistency and code review clarity
- Used Stopwatch.Frequency / 1_000_000L for tick-to-microsecond conversion (integer division, no floating point) matching RESEARCH.md recommendation
- Rolling 500-sample Queue with Dequeue-on-overflow keeps memory bounded without requiring a fixed array

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. Build succeeded on first attempt (0 errors). All pre-existing warnings (CA2014, CS0649, CS0414) are unrelated to these changes and were present before this plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- WPF baseline measurement is now fair: per-frame GC pressure removed from OpenXRService path
- ProximityRadarOverlay will log `[ProximityRadar WPF baseline]` Debug lines automatically during any VR session with 500+ frames
- Ready for Plan 01.1-02: SkiaSharp evaluation (implement equivalent render path, compare against this baseline)

---
*Phase: 01.1-render-tech-evaluation*
*Completed: 2026-05-19*
